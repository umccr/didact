/*import SitesService from '../sites.service';
import { setupCollectionSitesDb } from '../../testing/setup-collection-sites-db';
import { setupLocalstack } from '../../testing/setup-localstack';

beforeAll(async () => {
  setupLocalstack();
  await setupCollectionSitesDb();
});

describe('Testing Site Search', () => {
  describe('[GET] /users', () => {
    it('response statusCode 200 / findAll', async () => {
      const ss = new SitesService();

      const l = await ss.list('NSW');

      console.log(l);
    });
  });
});
*/
